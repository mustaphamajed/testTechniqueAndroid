<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testtechnique">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestTechnique">
        <activity android:name=".DashboardActivity"></activity>
        <activity android:name=".CongratsSecondActivity" />
        <activity android:name=".CongratsFirstActivity" />
        <activity android:name=".AddChildActivity" />
        <activity android:name=".ConfirmProfileActivity" />
        <activity android:name=".AddProfileActivity" />
        <activity android:name=".AddDeviceActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".SpaceChoseActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>